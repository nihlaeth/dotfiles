alias reload!='. ~/.zshrc'

# speech recognition aliases
alias e='vim'
alias add='git add'
alias commit='git commit -m'
alias status='git status'
alias push='git push'

# package manager aliases
alias spu='slackpkg -batch=on -default_answer=n update; slackpkg upgrade-all'
